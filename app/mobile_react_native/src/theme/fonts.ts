/**
 * Loaded in `app/_layout.tsx` via `useFonts` — use these `fontFamily` string values in styles.
 * Pairing with [Wolt-clone](https://github.com/Wealthometer/Wolt-clone) Nunito usage.
 */
export const fontFamily = {
  regular: "Nunito_400Regular",
  semiBold: "Nunito_600SemiBold",
  bold: "Nunito_700Bold",
  /** Matches [Wolt-clone](https://github.com/Wealthometer/Wolt-clone) `Fonts.brandBlack` — large discovery titles. */
  black: "Nunito_900Black",
} as const;
