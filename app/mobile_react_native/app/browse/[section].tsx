import { BrowseSectionScreen } from "@/features/home/screens/BrowseSectionScreen";

/** URL: `/browse/nearby` | `/browse/top-rated` | `/browse/around-you` — full roster from home carousels. */
export default function BrowseSectionRoute() {
  return <BrowseSectionScreen />;
}
