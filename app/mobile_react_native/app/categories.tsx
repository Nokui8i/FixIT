import { CategoriesBrowseScreen } from "@/features/home/screens/CategoriesBrowseScreen";

/** URL: `/categories` — full grid of service categories (Wolt-style discovery). */
export default function CategoriesRoute() {
  return <CategoriesBrowseScreen />;
}
