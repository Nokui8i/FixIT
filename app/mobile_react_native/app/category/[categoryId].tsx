import { CategoryFreelancersScreen } from "@/features/home/screens/CategoryFreelancersScreen";

/** URL: `/category/:categoryId` — browse freelancers for one service type (from home categories). */
export default function CategoryBrowseRoute() {
  return <CategoryFreelancersScreen />;
}
