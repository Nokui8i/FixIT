import { AccountManagementScreen } from "@/features/account/screens/AccountManagementScreen";

/** URL: `/account-management` — personal data, photo, legal & device links (Wolt-style “חשבון”). */
export default function AccountManagementRoute() {
  return <AccountManagementScreen />;
}
