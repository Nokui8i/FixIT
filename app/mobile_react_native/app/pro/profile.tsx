import { ProProfileEditorScreen } from "@/features/pro/screens/ProProfileEditorScreen";

/** URL: `/pro/profile` — edit the customer-facing marketplace profile. */
export default function ProProfileRoute() {
  return <ProProfileEditorScreen />;
}
