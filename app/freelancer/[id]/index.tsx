import { FreelancerProfileScreen } from "@/features/home/screens/FreelancerProfileScreen";

/** Customer view: `/freelancer/:id` — bio + portfolio preview (not the `/pro` workspace). */
export default function FreelancerProfileRoute() {
  return <FreelancerProfileScreen />;
}
