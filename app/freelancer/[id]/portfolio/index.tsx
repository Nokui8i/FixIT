import { FreelancerPortfolioGalleryScreen } from "@/features/home/screens/FreelancerPortfolioGalleryScreen";

/** `/freelancer/:id/portfolio` — all albums (preview + open per album). */
export default function FreelancerPortfolioGalleryRoute() {
  return <FreelancerPortfolioGalleryScreen />;
}
